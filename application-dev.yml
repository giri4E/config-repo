default:
  customer:
    value:
      - dev1
      - dev2
      - dev3

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info

logging:
  level:
    org.springframework.cloud.config: INFO